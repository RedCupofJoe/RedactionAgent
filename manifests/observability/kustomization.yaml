apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: lab-observability

resources:
  - namespace.yaml
  - tempo-monolithic.yaml
  - tempo-rbac.yaml
  - otel-collector.yaml
  - README-configmap.yaml
  # Cluster-scoped — applied without kustomize namespace stamp:
  #   oc apply -f manifests/observability/uiplugin-distributed-tracing.yaml
