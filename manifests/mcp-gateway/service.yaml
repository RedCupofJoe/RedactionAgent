apiVersion: v1
kind: Service
metadata:
  name: mcp-gateway
  namespace: mcp-gateway
  labels:
    app: mcp-gateway
spec:
  selector:
    app: mcp-gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
