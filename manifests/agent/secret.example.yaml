# Example only — do not put real passwords here.
# Generate gitignored secrets with:
#   ./scripts/generate_local_secrets.sh
#   ./scripts/generate_local_secrets.sh --apply
apiVersion: v1
kind: Secret
metadata:
  name: redaction-secrets
  namespace: redaction-agent
type: Opaque
stringData:
  S3_ACCESS_KEY: CHANGE_ME
  S3_SECRET_KEY: CHANGE_ME
  LLM_API_KEY: unused
  EMBEDDING_API_KEY: unused
