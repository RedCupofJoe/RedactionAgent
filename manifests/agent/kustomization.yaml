apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: redaction-agent

resources:
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - route.yaml
  - resourcequota.yaml
